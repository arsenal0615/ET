---
name: 'qx-gd-refine'
description: '中文文本精炼。诊断→精炼→验证，将松散文本重写为精确、无歧义、零冗余的文本。'
---

Invoke the `qx-gd-refine` skill to refine Chinese text.

对当前选中的文本或接下来提供的内容执行文本精炼。

**写入控制：本命令仅产出分析与建议，禁止自行写入或修改任何文件。如需将结果写入文档，必须先向用户展示完整内容并获得明确许可后才可执行。**

Pass any arguments as context: $ARGUMENTS
