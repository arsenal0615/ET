---
name: 'qx-gd-prototype'
description: 'HTML 原型验证。将策划文档转化为可交互的单文件 HTML 原型，验证功能覆盖度。'
---

Invoke the `qx-gd-prototype` skill to create an interactive HTML prototype from a design document.

对当前选中的文本或接下来提供的策划文档内容创建 HTML 原型。

Pass any arguments as context: $ARGUMENTS
