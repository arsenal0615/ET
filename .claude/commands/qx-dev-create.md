---
name: 'qx-dev-create'
description: '创建新变更并编写提案。用法: /qx-dev-create [name] 或 /qx-dev-create spec'
---

Invoke the `qx-dev-create` skill.

Subcommand and arguments: $ARGUMENTS

Usage:
- `/qx-dev-create <name>` — Create a new change with proposal
- `/qx-dev-create spec` — Define detailed requirements for current change
